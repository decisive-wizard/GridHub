(function() {
  angular
    .module('app.git')
    .factory('git', git);

    git.$inject = ['$rootScope'];

    function git($rootScope) {

      var service = {

      };

      return service;

      ////////////

    };
})();